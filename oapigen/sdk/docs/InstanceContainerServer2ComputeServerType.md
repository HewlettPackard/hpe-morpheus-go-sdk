# InstanceContainerServer2ComputeServerType

## Properties

Name | Type | Description | Notes
------------ | ------------- | ------------- | -------------
**Id** | Pointer to **NullableInt64** |  | [optional] 
**Code** | Pointer to **NullableString** |  | [optional] 
**Name** | Pointer to **NullableString** |  | [optional] 
**Managed** | Pointer to **NullableBool** |  | [optional] 
**ExternalDelete** | Pointer to **NullableBool** |  | [optional] 
**Format** | Pointer to **NullableString** |  | [optional] 

## Usage

Instantiate with a Go composite literal:

```go
obj := &InstanceContainerServer2ComputeServerType{
    // Set fields directly
}
```

### Id (Nullable)

Use the Nullable wrapper methods:
- `obj.Id.IsSet()` — check if set
- `obj.Id.Get()` — get the inner value (returns pointer)
- `obj.Id.Set(&val)` — set the value
- `obj.Id.Unset()` — clear the value
### Code (Nullable)

Use the Nullable wrapper methods:
- `obj.Code.IsSet()` — check if set
- `obj.Code.Get()` — get the inner value (returns pointer)
- `obj.Code.Set(&val)` — set the value
- `obj.Code.Unset()` — clear the value
### Name (Nullable)

Use the Nullable wrapper methods:
- `obj.Name.IsSet()` — check if set
- `obj.Name.Get()` — get the inner value (returns pointer)
- `obj.Name.Set(&val)` — set the value
- `obj.Name.Unset()` — clear the value
### Managed (Nullable)

Use the Nullable wrapper methods:
- `obj.Managed.IsSet()` — check if set
- `obj.Managed.Get()` — get the inner value (returns pointer)
- `obj.Managed.Set(&val)` — set the value
- `obj.Managed.Unset()` — clear the value
### ExternalDelete (Nullable)

Use the Nullable wrapper methods:
- `obj.ExternalDelete.IsSet()` — check if set
- `obj.ExternalDelete.Get()` — get the inner value (returns pointer)
- `obj.ExternalDelete.Set(&val)` — set the value
- `obj.ExternalDelete.Unset()` — clear the value
### Format (Nullable)

Use the Nullable wrapper methods:
- `obj.Format.IsSet()` — check if set
- `obj.Format.Get()` — get the inner value (returns pointer)
- `obj.Format.Set(&val)` — set the value
- `obj.Format.Unset()` — clear the value

[[Back to Model list]](../README.md#documentation-for-models) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to README]](../README.md)


